name: Test_telethon

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action
        uses: actions/checkout@v2
      - name: run
        run: |
          echo $super_secret
          python3 -c "print('abc')"
        env: # Or as an environment variable
          super_secret: ${{ secrets.ARG_A }}
